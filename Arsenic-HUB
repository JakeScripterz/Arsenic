if game.PlaceId == 10449761463 then
   local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
   
   local Window = Rayfield:CreateWindow({
      Name = "Arsenic Hub",
      Icon = 0, 
      LoadingTitle = "Arsenic Hub Is Loading",
      LoadingSubtitle = "by JacKScripterz",
      Theme = "Default",
      DisableRayfieldPrompts = false,
      DisableBuildWarnings = false, 
      ConfigurationSaving = {
         Enabled = false,
         FolderName = nil, 
         FileName = "Skibidi_Scripterz_hub"
      },
      Discord = {
         Enabled = false,
         Invite = "noinvitelink", 
         RememberJoins = true 
      },
      KeySystem = false, 
      KeySettings = {
         Title = "Untitled",
         Subtitle = "Key System",
         Note = "No method of obtaining the key is provided", 
         FileName = "Key", 
         SaveKey = true, 
         GrabKeyFromSite = false, 
         Key = {"Hello"} 
      }
   })
   
   local MainTab = Window:CreateTab("🏠Home", nil)
   local MainSection = MainTab:CreateSection("Custom Movesets")
   
   Rayfield:Notify({
      Title = "Made By JacKScripterz",
      Content = "Welcome to Arsenic Hub",  -- Added missing comma
      Duration = 5,
      Image = nil
   })
   
   -- Your existing buttons remain the same...
   
   local MainSection2 = MainTab:CreateSection("Miscellaneous")

   -- Create sliders in MainSection2
   local WalkSpeedSlider = MainTab:CreateSlider({
      Name = "Walk Speed",
      Info = "Adjusts player walk speed", -- Added Info
      Min = 16,
      Max = 100,
      Default = 16,
      Color = Color3.fromRGB(255,255,255),
      Increment = 1,
      ValueName = "Speed",
      Callback = function(Value)
         game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
      end    
   })

   local JumpPowerSlider = MainTab:CreateSlider({
      Name = "Jump Power",
      Info = "Adjusts player jump power", -- Added Info
      Min = 50,
      Max = 200,
      Default = 50,
      Color = Color3.fromRGB(255,255,255),
      Increment = 1,
      ValueName = "Power",
      Callback = function(Value)
         game.Players.LocalPlayer.Character.Humanoid.JumpPower = Value
      end    
   })

   Rayfield:LoadConfiguration()
end
